<Window x:Class="Projekt_kurier.NormalUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NormalUserWindow" Height="621.5" Width="600" MinHeight="321.5" MinWidth="300" MaxHeight="621.5" MaxWidth="600"
        WindowStartupLocation="CenterScreen" SizeChanged="SqareResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Button Click="SendParcel" Style="{StaticResource NormalUserWindowSendParcel}" ToolTip="Nadaj przesyłkę">Nadaj Przesyłkę</Button>
        <Button Grid.Column="1" Click="ShowList" Style="{StaticResource NormalUserWindowCheckStatus}" ToolTip="Sprawdź status przesyłki">Sprawdź stan przesyłki</Button>
        <Button Grid.Row="1" Click="SendersPersonalData" Style="{StaticResource NormalUserWindowPersonalData}" ToolTip="Wprowadź dane nadawcy">Dane Osobowe</Button>
        <Button Grid.Row="1" Grid.Column="1" Click="Logout" Style="{StaticResource Logout}" ToolTip="Wyloguj">Logout</Button>
    </Grid>
</Window>
